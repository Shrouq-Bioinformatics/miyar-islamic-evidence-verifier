# مِعيار — ملف المشاركة

## بطاقة مختصرة

| البند | البيان |
|---|---|
| الاسم | مِعيار — مدقّق الأدلة الإسلامية |
| المسار | **المسار الرابع: أدوات المعرفة والتحقق لتمكين المعرّفين بالإسلام** |
| الجمهور | فرق المحتوى الدعوي والتربوي، المحررون، والمراجعون |
| النطاق | فرز أولي قابل للتتبع قبل النشر، لا فتوى ولا اعتماد نهائي |
| حالة النسخة | نموذج محلي قابل للعرض؛ غير جاهز للإطلاق أو ادعاء الدقة |

## المشكلة

تنتقل ادعاءات إسلامية إلى مواد دعوية وتعليمية مع إحالات ناقصة أو خلط بين النص والتفسير والرأي والفتوى. البحث اليدوي بطيء، وواجهة «إجابة واحدة» قد تخفي غياب المصدر. يحتاج الفريق إلى مسار يجيب أولًا: هل لدينا دليل مسترجع فعلًا؟ ما درجة كفايته؟ ومتى يجب إيقاف النشر وإحالة الحالة إلى مختص؟

## الحل والقيمة

مِعيار مساحة عربية RTL تستقبل الادعاء كما ورد، وتعرض حالة الدليل والخطوة التالية، وتحفظ قرار المراجع محليًا. التصميم المقصود يفصل أربع طبقات:

1. **الادعاء والسياق** كما أدخلهما المستخدم.
2. **الاسترجاع والمطابقة** مع مصدر منقح — طبقة مستهدفة وليست منفذة حاليًا.
3. **الفرز الأولي** إلى مدعوم مبدئيًا، يحتاج مراجعة، أو أدلة غير كافية.
4. **القرار البشري** قبل الاعتماد أو النشر.

القيمة الأساسية ليست «الإجابة الأسرع»، بل جعل لحظة التوقف وعدم اليقين واضحة وقابلة للمراجعة.

## ما هو منفذ الآن

- لوحة قيادة، نموذج تدقيق، سجل حالات، غرفة مراجعة، مكتبة مصادر، تحليلات وإعدادات أمان.
- API عند `POST /api/verify`.
- فهرس وصفي لمصادر أولية وروابط خارجية.
- قيود لمعرفات المصادر في مخرجات المزود.
- وضع محلي معلن: يرصد ألفاظ الإحالة أو المسائل المعاصرة، لا يجلب نصًا، لا يثبت نسبة، لا يعرض ثقة رقمية.
- حفظ الحالات والأنشطة والمصادر المحفوظة في `localStorage`.

## المنهجية

### المسار الحالي

1. التحقق من طول المطالبة.
2. إذا غاب مفتاح صالح أو رفضه المزود، تشغيل قواعد محلية شفافة.
3. القواعد ترصد مؤشرات مثل أسماء المصادر أو ألفاظ الفتوى/المسائل المعاصرة.
4. النتيجة المحلية لا تكون «مدعوم»: إما `needs_review` أو `insufficient`، بثقة صفر وإحالات فارغة.
5. تعرض الواجهة سبب التوقف، وإجراءً مقترحًا، وتنبيه أن الحفظ محلي.

### مسار المزود التجريبي

يرسل الخادم الادعاء والسياق وفهرسًا وصفيًا إلى `gpt-4.1-mini` ويطلب JSON مقيدًا، ثم يصفي معرفات المصادر غير المعروفة ويضبط القيم. هذا **فرز لغوي أولي فقط**: لا توجد طبقة retrieval، ولا اقتباسات مسترجعة، ولا تحقق من نص المصدر. لذلك لا تكفي نتيجة `supported` — إن ظهرت بعد إصلاح المفتاح — للنشر.

### المنهجية المستهدفة قبل التجربة الميدانية

استرجاع نصوص موثقة ومؤرخة → مطابقة الاقتباس والسياق → إظهار المقطع والرابط والنسخة → فرز محافظ → مراجعة بشرية مزدوجة للحالات الحساسة → حفظ قرار وسجل تدقيق.

## القيود والمخاطر

- مفتاح الذكاء الاصطناعي الحالي مرفوض، وصاحب المشروع رفض استبداله؛ العرض المحلي هو المسار الفعلي الآن.
- لا retrieval أو قاعدة نصوص؛ مكتبة المصادر بطاقات وروابط وليست دليلًا مسترجعًا.
- لا مجموعة اختبار محكمة ولا نتائج دقة/استدعاء/اتفاق مراجعين.
- الأرقام والبذور الظاهرة في الواجهة للعرض، وليست قياسات تشغيلية حقيقية.
- الحفظ في المتصفح فقط؛ لا مزامنة، نسخ احتياطي، ملكية فريق، أو سجل تدقيق غير قابل للتلاعب.
- لا تسجيل دخول، أدوار، صلاحيات فرق، عزل مستأجرين، أو إدارة جلسات.
- CORS مفتوح في الخادم الحالي، ولا توجد ضوابط إنتاجية للحد من الطلبات.
- لا سياسة احتفاظ منفذة ولا آلية حذف مركزي؛ مسح بيانات الموقع يحذف السجل المحلي.
- رابط العرض العام متاح: `https://maayr-islamic-evidence-verifier.replit.app`، لكن آخر تغييرات workspace تحتاج إعادة نشر قبل التسليم.
- مستودع GitHub العام: https://github.com/Shrouq-Bioinformatics/miyar-islamic-evidence-verifier
- العربية ولهجاتها، اختلاف الطبعات، صحة الحديث، والسياق الفقهي تحتاج خبراء ومصادر مرخصة.

## بروتوكول التقييم المقترح — لم يُنفذ بعد

1. **مجموعة ذهبية:** 120–200 حالة مجهولة المصدر للنظام، موزعة بالتساوي على آيات محددة، أحاديث منسوبة، أسئلة فقهية معاصرة، تعميمات بلا مصدر، وحالات ملتبسة/مضللة.
2. **الترميز:** مراجعان مستقلان من أهل الاختصاص يحددان الحالة، المرجع المقبول، سبب الإحالة، وخطورة الخطأ؛ يحسم ثالث الخلاف.
3. **خط أساس:** البحث اليدوي المعتاد للفريق، مع قياس الزمن وجودة الإحالة.
4. **اختبار أعمى:** ترتيب عشوائي، دون إظهار التصنيف الذهبي، وفصل بيانات الضبط عن الاختبار.
5. **مؤشرات:** دقة الإحالات النصية، استدعاء حالات «يحتاج مراجعة»، معدل الدعم الزائف، معدل الامتناع، الوسيط الزمني للوصول إلى إحالة قابلة للتتبع، واتفاق المراجعين (Cohen’s κ).
6. **بوابة أمان مقترحة:** صفر إحالة مختلقة في مجموعة الاختبار، واستدعاء ≥95% للحالات الحساسة، ومراجعة بشرية لـ100% من الفتاوى/المسائل المعاصرة. لا يُعلن رقم نجاح قبل تنفيذ القياس وتوثيقه.
7. **اختبارات إساءة:** حقن أوامر، اقتباس مبتور، تشابه أسماء السور/الأحاديث، لغات مختلطة، وبيانات شخصية داخل المطالبة.

## الخصوصية والتعامل مع البيانات

### الواقع الحالي

- المدخلات والنتائج تحفظ في `localStorage` على الجهاز نفسه.
- عند مسار المزود الصالح، يرسل الخادم المطالبة والسياق واللغة إلى OpenAI؛ لا توجد الآن شاشة موافقة تفصيلية أو حذف مركزي.
- لا مصادقة؛ أي شخص يستخدم ملف المتصفح نفسه قد يرى البيانات المحلية.
- يجب استخدام بيانات عامة/مصطنعة فقط، وعدم إدخال أسماء أشخاص، محادثات خاصة، معلومات صحية، أو أسرار.

### ضوابط مطلوبة قبل النشر

موافقة صريحة قبل الإرسال الخارجي، تقليل البيانات وتنقيح PII، تشفير أثناء النقل وعند التخزين، أدوار وصلاحيات، مدد احتفاظ وحذف، سجل تدقيق، اتفاقية معالجة مع المزود، تحديد الإقليم، واستجابة للحوادث. ينبغي توثيق مصادر النصوص وتراخيصها ونسخها.

## إعادة الإنتاج والنشر

المتطلبات: Node.js 24 وpnpm.

```bash
pnpm install --frozen-lockfile
pnpm run typecheck
PORT=24752 BASE_PATH=/ pnpm --filter @workspace/miyar-evidence-verifier run build
pnpm --filter @workspace/api-server run build
```

تشغيل الخدمات وفق artifact router في Replit:

```bash
PORT=8080 pnpm --filter @workspace/api-server run dev
PORT=24752 BASE_PATH=/ pnpm --filter @workspace/miyar-evidence-verifier run dev
```

تتوقع الواجهة `/api/verify` على الأصل نفسه؛ لذا يحتاج التشغيل خارج Replit إلى reverse proxy يجمع الواجهة و`/api`. لا يلزم `DATABASE_URL` لمسار المنتج الحالي، رغم وجود حزمة DB هيكلية في workspace. لا تُضمّن `OPENAI_API_KEY` في كود أو أرشيف؛ وبدونه يعمل الوضع المحلي. النشر العام الحالي autoscale على `https://maayr-islamic-evidence-verifier.replit.app` وبناؤه ناجح، لكن يجب إعادة نشر آخر تغييرات workspace ثم إعادة اختبار الرابط من نافذة خاصة.

## تدفق عرض 90 ثانية

انظر `docs/demo-script-90s.md`. الخلاصة: المشكلة (0–15)، لوحة القيادة (15–28)، حالة إحالة واضحة في التدقيق (28–55)، غرفة المراجعة (55–70)، المصادر والحدود (70–83)، الخاتمة الصادقة (83–90).

## قائمة التسليم والجاهزية

### مكتمل في هذه الحزمة

- [x] تعريف المشكلة والحل والمسار الرابع.
- [x] منهجية وحدود واضحة بلا ادعاء تحقق نهائي.
- [x] بروتوكول تقييم مقترح وسياسة بيانات حالية/مستهدفة.
- [x] أوامر إعادة إنتاج مطابقة للـworkspace الحالي.
- [x] نص تعليق صوتي 90 ثانية بتوقيتات.
- [x] موجز HTML عربي ذاتي الاحتواء ومهيأ للطباعة، ونسخة PDF.
- [x] مصدر عرض من 16 شريحة، وتصديران سابقان بصيغتي PDF وPowerPoint محفوظان دون استبدال.
- [x] أرشيف مصدر بقائمة سماح يستبعد الأسرار و`node_modules` و`.git` و`attached_assets`.
- [x] رابط Live Demo عام وبناء نشر ناجح.
- [x] فيديو MP4 أفقي بنسبة 16:9 ومدته 87 ثانية.
- [x] جرد آلي لتراخيص تبعيات الإنتاج.
- [x] ملف مطابقة شروط المسابقة وجرد التراخيص مضمنان في حزمة المصدر.

### حواجز تمنع ادعاء الجاهزية النهائية

- [ ] **محجوب:** مفتاح AI صالح؛ المفتاح الحالي مرفوض ولم يُستبدل.
- [ ] **غير منفذ:** retrieval ومطابقة اقتباسات من مصادر موثقة.
- [ ] **غير منفذ:** تقييم محكم ونتائج منشورة.
- [ ] **غير منفذ:** مصادقة وصلاحيات فرق وتخزين خادمي وسجل تدقيق.
- [ ] **يلزم تحديثه:** إعادة نشر آخر تغييرات workspace واختبار رابط العرض العام من جهاز/نافذة نظيفة.
- [ ] **محجوب بالتصدير:** إعادة تصدير العرض من المصدر المعزّز؛ `exportSlides` يفشل حاليًا، والتصديران الموجودان يسبقان آخر تحسينات المصدر.
- [x] **مكتمل:** مستودع GitHub عام يحتوي حزمة المصدر المفحوصة.
- [x] **مكتمل:** فيديو MP4 بنسبة 16:9 ومدته 87 ثانية؛ يلزم فقط إرفاقه في نموذج المسابقة.
- [ ] **يلزم قبل الإرسال الرسمي:** مراجعة شرعية/قانونية، تدقيق PDF، ملء بيانات الفريق وروابط المشاركة، وتجربة النشر من جهاز نظيف.

**القرار:** الحزمة الوثائقية قابلة للمراجعة والطباعة، والنموذج قابل للعرض المحلي ضمن حدوده؛ المشروع ليس جاهزًا بعد لادعاء تحقق فعلي أو إطلاق إنتاجي.